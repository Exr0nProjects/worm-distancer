import update from "react-addons-update";

export const INITAL_STATE = {
    datas: []
}

export function reducer_fn(state, { action, payload }) {
    switch (action) {
        case 'change_frame':

    }
}
